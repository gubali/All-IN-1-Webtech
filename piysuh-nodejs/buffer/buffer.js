const {Buffer} = require('buffer');
// const buf = Buffer.alloc(8);
// console.log(buf[1]);
// const buf = Buffer.from("Hello!");
// console.log(buf);
// console.log(buf.toString());
// const bufTwo = Buffer.allocUnsafe

const buf = Buffer.from("chai");
console.log(buf.length);

